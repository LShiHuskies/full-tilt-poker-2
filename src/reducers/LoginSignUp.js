import { CREATE_USER } from '../actions/CreateLoginActions';

const defaultState = {
  user: null
}

export default function LoginOrSignUp(state = defaultState, action) {
    switch(action.type) {

    }
}